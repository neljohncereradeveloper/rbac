/**
 * Core feature entities
 * Export all entities for core functionality
 */

import { ActivityLogEntity } from './activity-log.entity';

/**
 * Array of Core entities for TypeORM configuration
 */
export const coreEntities = [ActivityLogEntity];
