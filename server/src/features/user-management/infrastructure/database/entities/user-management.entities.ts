/**
 * User Management feature entities
 * Export all entities for this feature
 */

import { UserEntity } from './user.entity';

/**
 * Array of User Management entities for TypeORM configuration
 */
export const userManagementEntities = [UserEntity];
