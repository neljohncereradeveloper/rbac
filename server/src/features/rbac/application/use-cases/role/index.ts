// Note: create.use-case, update.use-case, archive.use-case, restore.use-case, get-by-id.use-case, get-paginated.use-case, combobox.use-case removed
// Roles are statically defined (Admin, Editor, Viewer) and managed via seeders only
// Roles are fetched without pagination
export * from './get-all.use-case';
