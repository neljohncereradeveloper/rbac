// Note: create.use-case, update.use-case, archive.use-case, restore.use-case, get-paginated.use-case, get-by-id.use-case, combobox.use-case removed
// Permissions are statically defined and managed via seeders only
// Permissions are fetched without pagination
export * from './get-all.use-case';
